name: Deploy to Azure Container Apps

on:
  push:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op workflow (simula CI)
        run: echo "Simulación exitosa para permitir merge"
